<UserControl x:Class="LearningWPF.View.RecipeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LearningWPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="669" d:DesignWidth="894">
    <Grid DataContext="{Binding SelectedRecipe}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <Label Content="{Binding NameRecipe, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Text="{Binding NameRecipe, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed"/>
        <Grid Grid.Row="1" Background="White">
            <Button Click="AddImage">
                <Image x:Name="Image1" Grid.Row="1" Source="Снимок экрана (6).png" />
            </Button>
        </Grid>
        <Label Grid.Row="2" Content="{Binding СompositionOfTheDish, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Grid.Row="2" Text="{Binding СompositionOfTheDish, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed"/>
        <Label Grid.Row="3" Content="{Binding CookingMethod, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox  Grid.Row="3" Text="{Binding CookingMethod, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed"/>

    </Grid>
</UserControl>
